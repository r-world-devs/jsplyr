# Select columns from `JSON` data.

Select columns from `JSON` data.

## Usage

``` r
# S3 method for class 'tbl_lazy_json'
select(.data, ...)
```

## Arguments

- .data:

  A `tbl_lazy_json` object.

- ...:

  Column names.
