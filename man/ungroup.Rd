% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-ungroup.R
\name{ungroup}
\alias{ungroup}
\alias{ungroup.tbl_lazy_json}
\title{Remove grouping from \code{JSON} data.}
\usage{
\method{ungroup}{tbl_lazy_json}(x, ...)
}
\arguments{
\item{x}{A \code{tbl_lazy_json} object.}

\item{...}{Columns to remove from the grouping. Accepts the same inputs as
\code{\link[=group_by]{group_by()}} (bare names, strings, and the tidyselect helpers \code{c(...)},
\code{all_of()}/\code{any_of()}, \code{across()}). If empty, all grouping is removed.}
}
\description{
Drops grouping previously set with \code{\link[=group_by]{group_by()}}. With no
arguments all grouping is removed; supplying column names removes only those
columns from the grouping set (partial ungroup), matching \code{\link[dplyr:ungroup]{dplyr::ungroup()}}.
}
\details{
Grouping is tracked as browser-side state consumed by group-aware
verbs such as \code{\link[=summarise]{summarise()}} and the \code{\link[=slice]{slice()}} family. \code{ungroup()} appends a
step that clears or trims that state at compute time.
}
\examples{
\dontrun{
tbl(session, "mtcars") |>
  group_by(cyl) |>
  ungroup()
}
}
