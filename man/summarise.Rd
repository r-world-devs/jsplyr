% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-summarise.R
\name{summarise}
\alias{summarise}
\alias{summarise.tbl_lazy_json}
\title{Summarise \code{JSON} data.}
\usage{
\method{summarise}{tbl_lazy_json}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{Name-value pairs of summary functions.
The name gives the name of the column in the output.
The value should be a call to a summary function like
\code{mean()}, \code{sum()}, \code{min()}, \code{max()}, \code{n()}.

\code{across()} is expanded on the R side into one summary per selected
column. It accepts a single function (\code{across(c(a, b), mean)}) or a
named list of functions (\code{across(c(a, b), list(mean = mean, sd = sd))}).
Column selections accept \code{c(...)}, a bare column, a character vector, and
\code{all_of()}/\code{any_of()}. Use \code{.names} with the \code{{.col}}/\code{{.fn}} glue
placeholders to control the output column names; by default a single
function reuses the input name and multiple functions produce
\verb{\{.col\}_\{.fn\}}.}
}
\description{
Summarise \code{JSON} data.
}
