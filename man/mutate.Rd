% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-mutate.R
\name{mutate}
\alias{mutate}
\alias{mutate.tbl_lazy_json}
\title{Add or modify columns in \code{JSON} data.}
\usage{
\method{mutate}{tbl_lazy_json}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{Name-value pairs of expressions.
The name gives the name of the column in the output.
The value should be an expression using existing columns,
e.g. \code{new_col = col1 + col2}.

Conditional helpers are translated to JavaScript:
\code{ifelse()} and \code{if_else()} become ternary operators, and
\code{case_when()} becomes a chained set of ternary operators.
\code{case_when()} clauses without a \code{TRUE ~ ...} catch-all yield \code{null}
for unmatched rows, matching \code{dplyr}'s \code{NA} default.

\code{across()} is expanded on the R side into one column per selection.
It accepts a single function (\code{across(c(a, b), round)}), a formula
lambda (\code{across(c(a, b), ~ .x * 2)}), or a named list of functions
(\code{across(c(a, b), list(double = ~ .x * 2))}). Column selections accept
\code{c(...)}, a bare column, a character vector, and \code{all_of()}/\code{any_of()}.
Use \code{.names} with the \code{{.col}}/\code{{.fn}} glue placeholders to control the
output column names; by default a single function reuses the input name
and multiple functions produce \verb{\{.col\}_\{.fn\}}.}
}
\description{
Add or modify columns in \code{JSON} data.
}
