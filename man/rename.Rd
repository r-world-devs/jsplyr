% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-rename.R
\name{rename}
\alias{rename}
\alias{rename.tbl_lazy_json}
\title{Rename columns of \code{JSON} data.}
\usage{
\method{rename}{tbl_lazy_json}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{Use \code{new_name = old_name} to rename columns. Both bare names and
character strings are accepted (e.g. \code{rename(mpg_new = mpg)} or
\code{rename("mpg_new" = "mpg")}). Column order is preserved.}
}
\value{
A \code{tbl_lazy_json} object with the rename appended as a lazy compute
step, applied in the browser when the pipeline is evaluated.
}
\description{
Rename columns of \code{JSON} data.
}
\examples{
if (interactive()) {
  tbl(session, "mtcars") |>
    rename(miles_per_gallon = mpg, cylinders = cyl)
}
}
