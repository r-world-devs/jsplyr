% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-relocate.R
\name{relocate}
\alias{relocate}
\alias{relocate.tbl_lazy_json}
\title{Change column order of \code{JSON} data.}
\usage{
\method{relocate}{tbl_lazy_json}(.data, ..., .before = NULL, .after = NULL)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{Columns to move. Bare names and character strings are accepted.}

\item{.before, .after}{Destination of the columns selected by \code{...}. Supply a
single column (bare name or string) to place the moved columns before or
after it. With neither, the selected columns move to the front.}
}
\description{
Change column order of \code{JSON} data.
}
\details{
Reordering happens in the browser by rebuilding each row's keys in
the new order. Columns not named in \code{...} keep their relative order.
}
\examples{
\dontrun{
tbl(session, "mtcars") |> relocate(gear, carb)
tbl(session, "mtcars") |> relocate(mpg, .after = cyl)
}
}
