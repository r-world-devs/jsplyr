% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-join.R
\name{joins}
\alias{joins}
\alias{left_join.tbl_lazy_json}
\alias{right_join.tbl_lazy_json}
\alias{inner_join.tbl_lazy_json}
\alias{full_join.tbl_lazy_json}
\alias{semi_join.tbl_lazy_json}
\alias{anti_join.tbl_lazy_json}
\title{Join two \code{JSON} tables.}
\usage{
\method{left_join}{tbl_lazy_json}(x, y, by = NULL, ...)

\method{right_join}{tbl_lazy_json}(x, y, by = NULL, ...)

\method{inner_join}{tbl_lazy_json}(x, y, by = NULL, ...)

\method{full_join}{tbl_lazy_json}(x, y, by = NULL, ...)

\method{semi_join}{tbl_lazy_json}(x, y, by = NULL, ...)

\method{anti_join}{tbl_lazy_json}(x, y, by = NULL, ...)
}
\arguments{
\item{x}{A \code{tbl_lazy_json} object (the left table).}

\item{y}{A \code{tbl_lazy_json} object (the right table). Must share the same
browser \code{session} as \code{x}.}

\item{by}{A character vector of columns to join by. Use a named vector
(e.g. \code{c("a" = "b")}) to match columns with different names in \code{x} and
\code{y}. If \code{NULL} (the default), a natural join is performed using all
columns common to both tables.}

\item{...}{Unused. Provided for consistency with the generics.}
}
\value{
A \code{tbl_lazy_json} object with the join appended as a compute step.
}
\description{
Mutating joins (\code{left_join}, \code{right_join}, \code{inner_join},
\code{full_join}) combine columns from \code{x} and \code{y}, matching rows by key
columns. Filtering joins (\code{semi_join}, \code{anti_join}) keep columns from
\code{x}, using \code{y} only to determine which rows to keep.
}
