% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-filter.R
\name{filter}
\alias{filter}
\alias{filter.tbl_lazy_json}
\title{Add filter to \code{JSON} data.}
\usage{
\method{filter}{tbl_lazy_json}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{Filtering expressions. Comparisons (\code{==}, \code{>}, \code{<}, etc.) combined
with \code{&}/\code{|} are supported, as well as the \code{dplyr} helpers \code{is.na()},
\code{between()}, and \code{across()}/\code{if_all()}/\code{if_any()}. \code{across()}/\code{if_all()} and
\code{if_any()} expand the predicate over the selected columns, combining them
with \code{&} and \code{|} respectively. Column selections accept \code{c(...)}, a bare
column, a character vector, and \code{all_of()}/\code{any_of()}. Values referenced
from \code{input} or the calling environment are resolved on the R side; column
references are evaluated in the browser.}
}
\description{
Add filter to \code{JSON} data.
}
