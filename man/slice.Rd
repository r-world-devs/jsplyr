% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-slice.R
\name{slice}
\alias{slice}
\alias{slice.tbl_lazy_json}
\alias{slice_head.tbl_lazy_json}
\alias{slice_tail.tbl_lazy_json}
\alias{slice_min.tbl_lazy_json}
\alias{slice_max.tbl_lazy_json}
\title{Select rows of \code{JSON} data by position.}
\usage{
\method{slice}{tbl_lazy_json}(.data, ..., .by = NULL, .preserve = FALSE)

\method{slice_head}{tbl_lazy_json}(.data, ..., n, prop, by = NULL)

\method{slice_tail}{tbl_lazy_json}(.data, ..., n, prop, by = NULL)

\method{slice_min}{tbl_lazy_json}(
  .data,
  order_by,
  ...,
  n,
  prop,
  by = NULL,
  with_ties = TRUE,
  na_rm = FALSE
)

\method{slice_max}{tbl_lazy_json}(
  .data,
  order_by,
  ...,
  n,
  prop,
  by = NULL,
  with_ties = TRUE,
  na_rm = FALSE
)
}
\arguments{
\item{.data}{A \code{tbl_lazy_json} object.}

\item{...}{For \code{slice()}, integer row positions to keep (1-based). Negative
positions drop rows. Unused by the other variants.}

\item{.preserve}{Ignored. Accepted for consistency with the generic.}

\item{n}{Number of rows to keep. Used by \code{slice_head()}, \code{slice_tail()},
\code{slice_min()}, and \code{slice_max()}. Defaults to 1 where applicable.}

\item{prop}{Proportion of rows to keep (0-1), an alternative to \code{n} for
\code{slice_head()}/\code{slice_tail()}/\code{slice_min()}/\code{slice_max()}.}

\item{by, .by}{Ignored. Accepted for consistency with the generics; per-call
grouping is not applied (use \code{\link[=group_by]{group_by()}}, which slicing respects).}

\item{order_by}{For \code{slice_min()}/\code{slice_max()}, the column to order by
(bare name or string).}

\item{with_ties, na_rm}{Ignored. Accepted for consistency with the
\code{slice_min()}/\code{slice_max()} generics.}
}
\description{
The \code{slice()} family picks rows by position or by ordering on a
column. When the data has been grouped with \code{\link[=group_by]{group_by()}}, slicing is
applied within each group.
}
\details{
All slicing is evaluated in the browser. \code{slice_min()}/\code{slice_max()}
order rows by the given column (ascending for min, descending for max) and
keep the first \code{n} (or \code{prop}).
}
\examples{
\dontrun{
tbl(session, "mtcars") |> slice(1, 3, 5)
tbl(session, "mtcars") |> slice_head(n = 5)
tbl(session, "mtcars") |> group_by(cyl) |> slice_max(mpg, n = 2)
}
}
